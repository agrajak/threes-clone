{
  "scripts": {
    "build": "parcel build index.html --public-url .",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^3.1.0",
    "jest": "^26.4.2",
    "typescript": "^4.0.2"
  }
}
